generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model bensgarage {
  id           Int       @id @default(autoincrement())
  date         DateTime?
  userid       String?
  errortype    String?
  errormessage String?
  userparam    String?
  usernote     String?
  stack        String?
}
